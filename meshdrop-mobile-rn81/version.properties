# Single source of truth for the app version, read by both the Android build
# (android/app/build.gradle) and scripts/make-latest-json.js.
#
# Bump versionCode on every release — the in-app updater compares this against
# the hosted manifest and only offers an update when the remote code is greater.
versionCode=44
versionName=1.0.44
